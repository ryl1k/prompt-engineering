# IMAGE TOKENS

Use image tokens only in this form:

[[image=N]]

Rules:
- Place each image token on its own line.
- Do not embed image tokens inside lists, tables, links, or text.
- Use only allowed IDs.
- Maximum 3 images per response.
